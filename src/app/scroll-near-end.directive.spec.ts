import { ScrollNearEndDirective } from './scroll-near-end.directive';

describe('ScrollNearEndDirective', () => {
  it('should create an instance', () => {
    const directive = new ScrollNearEndDirective();
    expect(directive).toBeTruthy();
  });
});
